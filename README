# Installing C2RN

To install c2rn user may wish to use the GNU Build system. Installations differs depending on
whether the GNU Autotools package is installed 

A) GNU Autotools not installed

mkdir build && cd build   
../configure   

B) GNU Autotools installed (prefered)

./autogen.sh
mkdir build && cd build
../configure
