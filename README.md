# Setup

#### Requirements: 
make, gcc, libopenssl, libgtk-3, pkg-config 

#### Installation:

```sh
cd src
./autogen.sh
mkdir build
cd build
../configure
make
```

#### Tested environments:
- Astra Linux 1.6 SE
